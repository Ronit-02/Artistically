# Security documentation boundary

This tracked file intentionally contains no vulnerability findings, security-control implementation details, verification evidence, deployment assumptions, or remediation history.

When present, `sensitive context/security-audit.local.md` holds the confidential audit record. It must be read explicitly; local context files are not loaded automatically. Never record live secrets in either file.
